# ---------------------------------------------------------------------------
# Quality Autopilot — Requirements
# ---------------------------------------------------------------------------
# Generated from pyproject.toml. Keep in sync.
# To regenerate: uv pip compile pyproject.toml -o requirements.txt
# ---------------------------------------------------------------------------

agno[os]
openai
anthropic
mcp
pgvector
psycopg[binary]
sqlalchemy
PyGithub
pydantic>=2.0

# Discovery Agent — UI Crawler
httpx
beautifulsoup4
curl_cffi

# Engineer Agent — Playwright MCP
requests
python-dotenv

# Data Agent — Dynamic Data Generation
faker

# Librarian Agent — File Watching
watchdog

# Knowledge Base Chunkers
chonkie[semantic]
unstructured
wikipedia

# Ollama Embedder
ollama

# Semantica — Semantic Layer, Decision Intelligence, Provenance
semantica[agno]

# CI/CD Dependencies
black
flake8
pytest
pytest-cov
pytest-asyncio
